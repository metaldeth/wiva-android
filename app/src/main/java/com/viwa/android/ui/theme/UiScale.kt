package com.viwa.android.ui.theme

/**
 * Глобальный масштаб Compose UI относительно системной density.
 * 0.9f = интерфейс на 10% меньше (и `.dp`, и `.sp` через density).
 */
const val GLOBAL_UI_SCALE = 0.9f
