# Ревью декомпозиции (самопроверка мета-оркестратора)

## Покрытие userTask

- Все ID A–F из ТЗ §4 распределены по подмодулям §6.2.
- Правило «B3 раньше B1+B2+B4» отражено волнами 2 и 3.
- Мок контроллера, OTA, источники §2 — явно в модулях A, B, C, D.

## Дублирование скоупа

- Нет пересечения: `stage-b-mock-first` только B3; `stage-b-real-controller` — B1, B2, B4.
- Телеметрия: транспорт (kiosk) и семантика (wiva) сосредоточены в `stage-d-telemetry` — корректно.

## Зависимости и циклы

- Граф линейный A → B3 → B124 → C → D → E → F; циклов нет.
- `stage-c-integrations` логически после каркаса A; опциональная связь с B для сценариев «оплата» — через последующий D/E, не создаёт цикл.

## Общие сущности

- Выделены контракт контроллера, телеметрия wiva, открытые продуктовые параметры §7.

## Риски

- В `wiva-android` пока нет `AGENTS.md` — первый complex должен заложить `buildCommand` (в рамках A или сразу после).
- Ответы на §7 ТЗ (applicationId, паритет UI, flavors, OTA URL) могут заблокировать аналитика на этапе A/C — оформлять как `blockingQuestions` по §4.1.

**Вердикт:** декомпозиция согласована с ТЗ, к исправлению не требуется до появления новых входных данных от владельца.
