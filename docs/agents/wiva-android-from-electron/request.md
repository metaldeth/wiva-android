# Запрос мета-сессии

**metaSessionId:** `wiva-android-from-electron`

**Источник:** команда пользователя `/Meta-complex` с привязкой к `wiva-android/docs/TZ_WIVA_ANDROID_FROM_ELECTRON.md`.

**userTask (сводка):**

- Реализовать нативное Android-приложение `wiva-android`, функционально согласованное с **wiva_electron**, с заимствованием интеграций и транспорта из **legacy Android kiosk** по правилам §2 ТЗ.
- Обязательный **мок контроллера** для эмулятора; полный перенос по модулям A–F; на каждом подмодуле — отдельный **complex**-пайплайн.
- Вопросы владельцу продукта — только по §4.1 ТЗ (контекст, противоречие, варианты для бизнеса, один чёткий вопрос).

**Порядок подмодулей:** таблица §6.2 ТЗ (`stage-a-carcass-ota` → … → `stage-f-office`).
