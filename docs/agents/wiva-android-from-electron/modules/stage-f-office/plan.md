# План выполнения: `stage-f-office` (F1–F2)

> Низкоуровневый план по `tz.md` и `architecture.md`. Код не планируется, кроме случаев инцидента на стенде (вне этих задач).

**Каноническое имя чеклиста (репо):** `wiva-android/docs/OFFICE_HARDWARE_CHECKLIST.md` — один файл на F1+F2.

**Эскалация:** при неоднозначности на стенде — вопрос владельцу по **§4.1** основного ТЗ, без произвольных решений.

## Таблица задач

| ID | Задача | Юзер-кейс (ТЗ) | Зависимости | Точки изменения (репозиторий) |
|----|--------|----------------|-------------|-------------------------------|
| **task-f1** | Шаблон офисного чеклиста (контроллер, мок↔реал, регрессия vs мок) + поля F1-R2; опционально ссылки в `AGENTS.md` | F1 подготовка/документирование (F1-R1, F1-R2) | — | **`OFFICE_HARDWARE_CHECKLIST.md`** в `wiva-android/docs/`; при необходимости указатель в `.../modules/stage-f-office/`; опционально `wiva-android/AGENTS.md`; `summary.md` в каталоге модуля |
| **task-f2** | Раздел «терминал» в том же чеклисте (шаблон F2-R1); поля под онсайт F2-O* | F2 фиксация в документе (репо = шаблон; онсайт = подпись) | — * | Тот же файл: **`wiva-android/docs/OFFICE_HARDWARE_CHECKLIST.md`** |

\*Оба артефакта — один Markdown-файл чеклиста. При **параллельной** работе: одна ветка/один PR или сначала merge скелета из task-f1, затем дополнение из task-f2 (без жёсткой волны 2, если команда объединяет коммиты).

## Волны выполнения

| Волна | Задачи | Примечание |
|-------|--------|------------|
| **1** | `task-f1`, `task-f2` | Достаточно **одной волны** для репозиторных deliverables: обе задачи — документация. Параллельно возможно при согласовании правок **одного** файла чеклиста (см. таблицу). |

## Вне скоупа волн

- **Онсайт F1-O\*, F2-O\*** — выполняет инженер стенда; закрытие по подписи и дате, не по CI.
- **`summary.md`** — итоговое заполнение раздела «Подтверждено на стенде» после прохождения стенда; в волне 1 — подготовка структуры/черновика по `architecture.md`.

## Сборка

Перед мержем PR с документацией — `buildCommand` из `wiva-android/AGENTS.md` (регрессия компиляции; этап F не добавляет обязательных изменений кода в этих задачах).
