# task-03 — code review, круг 3 (Arcus2 / AqsiRepositoryImpl)

**SessionId:** `wiva-aqsi-card-payment`  
**Объект:** A3 TCP-клиент, репозиторий, парсеры, диагностика, тесты.  
**Код не изменялся** (review-only).

**Процесс:** skill `code-reviewer-complex` предполагает цепочку `reviewAgents` из `AGENTS.md` через `review-orchestrator`; в `wiva-android/AGENTS.md` списка `reviewAgents` нет — выполнено **единое техническое ревью** по ТЗ task-03, результатам тестового отчёта и явной сверке замечаний круга 2.

**Тесты:** по `task-03-test-report.md` пакет `com.wiva.android.data.payment.aqsi` и `assembleDebug` проходят.

---

## Итоговая оценка (круг 3)

Замечания круга 2 по **guard/test для `buildPaymentOperationStart`**, **обоснованию отказа от хрупкого юнита на отмену** и **заметке про PCB в TCP-фикстурах** закрыты. Критических дефектов не выявлено (`hasCriticalIssues: false`). **Задачу по коду task-03 принимаю** для передачи в оркестратор как готовую с точки зрения обязательного ревью; остаётся лишь опциональная активность уровня «глубже свериться с KB в PDF» без влияния на текущую реализацию.

---

## Сверка с пунктами круга 2 (после некритичных исправлений)

| Тема (круг 2) | Статус круга 3 |
|---------------|----------------|
| **`buildPaymentOperationStart` без узкого guard** | **Закрыто.** В `Arcus2Client.kt` добавлены `require(amountKopecks >= 0)` с явным текстом ошибки и KDoc о двойной линии защиты (публичные пути + внутренний вызов). Юнит: `Arcus2ProtocolParsingTest.buildPaymentOperationStart_rejectsNegativeKopecks`. |
| **Отдельный юнит на `invokeOnCompletion` + `CancellationException`** | **Закрыто по решению с явным rationale.** В `task-03-test-report.md` добавлен подпункт про нестабильность синхронного блокирующего мока с `Dispatchers.IO` без «спящих» потоков/таймаутов; указано, что цепочка «закрыть TCP → выйти из read» покрыта `Arcus2ClientTest.initiatePurchase_interruptTcp_closesSocketAndFailsFast`, а wiring репозитория остаётся проверяемым по исходнику — это достаточно для круга ревью. |
| **PCB в `Arcus2ClientTest`** | **Закрыто.** Комментарий у фикстуры сервера в `Arcus2ClientTest.kt` описывает `pcb = 0`, отсутствие строгой валидации чередования на приёме и границу юнит vs железо; расширенный блок «PCB в TCP-фикстурах сервера» в `task-03-test-report.md`. |

Остальные сильные стороны и чеклист соответствия task-03 совпадают с кругом 2 без регрессий по прочитанному коду.

---

## Опционально (не блокируют приёмку)

- По желанию: отдельные приёмочные сценарии по PCB против живого терминала / KB — вне объёма обязательного unit-слоя task-03.
- Как ранее: полная побайтовая сверка всех фикстур с PDF KB в этом раунде не выполнялась.

---

## Классификация: критические проблемы

**Нет.**

---

**Reviewer role:** code-reviewer-complex (единый проход по ТЗ и закрытию круга 2).  
**Дата:** 2026-05-12  
**Круг:** 3 из 5
