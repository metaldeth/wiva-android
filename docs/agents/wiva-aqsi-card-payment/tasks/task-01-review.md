# task-01 — code review, круг 2 (session `wiva-aqsi-card-payment`)

**Дата:** 2026-05-12  
**Объект:** модель `CardPaymentMethod`, ключ `JsonStoreKeys`, `CardPaymentMethodRepository` + impl, unit-тесты.  
**Контекст:** повторное ревью после правок по кругу 1; **Hilt / `@Binds`** до **task-04** не рассматриваются (по указанию сессии).

**Тесты (внешний отчёт):** `task-01-test-report.md` — `:app:testDebugUnitTest` (фильтр на `CardPaymentMethodRepositoryTest`) и `assembleDebug` OK.

**Метод:** целевой проход по ТЗ, документации в коде и тестам; код не изменялся.

---

## Замечания круга 1 — статус

| Было (круг 1) | Статус |
|---------------|--------|
| Добавить краткий KDoc на `CardPaymentMethodRepository` | **Исправлено:** блок KDoc описывает строки хранения, ссылки на константы и дефолт `Pax`. |
| Отдельный тест на явно сохранённую пустую строку `""` | **Исправлено:** `getSelected_whenEmptyStringStored_returnsPax`. |

---

## Соответствие ТЗ и запретам

| Критерий | Оценка |
|----------|--------|
| Запрещённые модули не затронуты | Выполнено по составу файлов задачи. |
| Только ключ `CARD_PAYMENT_METHOD` (`"cardPaymentMethod"`) | Выполнено. |
| Sealed PAX / AQSI, `"PAX"` / `"AQSI"` в сторе | Выполнено. |
| Дефолт PAX при отсутствии / неизвестном / пустом после trim | Выполнено; поведение для `""` зафиксировано тестом. |
| Паттерн `ConfigRepository` (`get` / `set`) | Выполнено. |

---

## Documentation (KDoc)

- **`CardPaymentMethod`:** класс/тип и `fromStorageString` документированы; формулировка про `null`, trim, регистр и fallback на `Pax` согласована с реализацией.
- **`JsonStoreKeys.CARD_PAYMENT_METHOD`:** KDoc указывает тип значения и допустимые литералы — достаточно для навигации.
- **`CardPaymentMethodRepository`:** KDoc круга 2 закрывает пробел domain-слоя; перекрёстные ссылки на модель и константы корректны.

Замечаний по содержанию KDoc для этапа A1 нет.

---

## Tests (в т.ч. пустые строки между методами)

- Между блоками `@Test` соблюдены **пустые строки** (одна между соседними тестами) — читаемость и типичный Kotlin-стиль выдержаны.
- Покрытие пунктов 1–5 из `task-01.md` сохранено; добавлены регистронезависимость и пустая строка в сторе — контракт явный.
- `FakeConfigRepository` по-прежнему покрывает контракт `ConfigRepository`.

---

## General / структура / производительность (кратко)

- Логика маппинга и fallback без изменений по смыслу; критических дефектов не выявлено.
- Слойность domain / data соблюдена; объём кода остаётся уместным без лишних абстракций.

---

## Итог (круг 2)

Исправления по кругу 1 (**KDoc репозитория**, **тест на `""`**) внесены корректно. Новых содержательных проблем в перечисленных файлах не обнаружено. **Критических замечаний нет.**

**Вердикт:** с точки зрения ревью этапа A1 — готово; проводка в DI остаётся вне скоупа этого круга (до task-04).
