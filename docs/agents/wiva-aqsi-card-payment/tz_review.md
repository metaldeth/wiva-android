# Ревью ТЗ: wiva-aqsi-card-payment (круг 2)

**Источники:** `request.md`, `tz.md`, `wiva-android/AGENTS.md`  
**Skill:** `tz-reviewer` (complex)  
**Круг:** 2 из 5 (проверка устранения замечаний круга 1)

---

## Краткий вывод

Замечания первого ревью **отражены в актуальной версии ТЗ**:

- **G4 / порядок сборок** — синхронизировано с `request.md`: обязательные `gradlew.bat assembleDebug` после **A2, A4, A5, A6**; после **A1, A3, A7** — по необходимости; финальная сборка перед закрытием MR указана (**§ декомпозиция**, **G4**).
- **A7 / теги** — в критерии **A7** явно включены **`CardPayment`**, **`AqsiRepo`**, **`Arcus2`**, **`AqsiSettings`**, добавлено требование для слоя настроек и ограничение байтового лога по debug.
- **A5 / SBP, freeMode, mock** — сформулировано проверяемо (не меняются, кроме инъекции оркестратора и замены вызова для карты).
- **Размещение UI** — зафиксирован предпочтительный пакет `.../service/tabs/` по **AGENTS.md** с исключением и явным указанием файлов входа (**§ По структуре**, **A6**).
- **VCOM/TCP и `AqsiConfig`** — явно указан объём задачи (**Arcus2 TCP/IP**), путь без TCP-туннеля исключён при отсутствии паттерна/требования.
- **Лог байтов Arcus2** — усилено в жёстких ограничениях (**п. 8**) и НФТ (**Arcus2 / PCI-гигиена**).

Юзер-кейсы **UC-1–UC-5**, актёры, жёсткие ограничения постановки, оркестратор и телеметрия **`method = CARD`** согласованы с `request.md`. **Новых содержательных замечаний** по полноте, противоречиям постановке и скиллу `tz-reviewer` **не выявлено.**

**Итог: ТЗ принято** (можно передавать в проектирование/реализацию complex-пайплайна).

Для **`AGENTS.md` проекта** поле **`browserTesting: true` не задано** — отдельный раздел «UI-сценарии для тестирования» по правилам скилла **не обязателен**.

---

## Критичные проблемы

**Не выявлено.**

---

## Некритичные замечания

**Новых содержательных.**  
Микро-детали (например, в нижнем списке `request.md` осталась фраза «`assembleDebug` после каждого подэтапа», тогда как в том же файле выше согласован список A2/A4/A5/A6) относятся к **постановке**, не к ТЗ; текущее **ТЗ и G4** опираются на уточнённый фрагмент — достаточно иметь в виду при правке `request.md` при желании полного выравнивания документов.

---

## Матрица проверки (кратко)

| Тема | Статус |
|------|--------|
| Закрытие замечаний круга 1 | Выполнено в `tz.md` |
| Запреты controller / PAX / СБП / вкладки / `PaymentMethod` / `IntegrationsModule` / только новые ключи | Согласовано с `request.md` |
| Скилл `tz-reviewer` (юзер-кейсы, актёры, приёмка, противоречия) | Выполнено |
| `browserTesting` | Не требуется для `wiva-android/AGENTS.md` |
