# Запрос (sessionId: viwa-android-g-calibration-cooking/modules/stage-g2-syrup-calibration)

Постановка: реализовать калибровку сиропов (концентрата) в wiva-android по образцу wiva_electron — сервис команд 0x52/0x09, пересчёт conversionFactor, сохранение в merge-конфиг, отправка cellVolumeImportTopic и cellStoreImportTopic, вкладка сервисного меню, мок контроллера, unit-тесты расчёта и тела команды.

Полный текст задачи передан в чате оркестратора (complex).
