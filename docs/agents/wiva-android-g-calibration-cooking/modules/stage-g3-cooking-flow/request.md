# Запрос модуля G3 — Процесс готовки напитка

Полная постановка: реализация `DrinkSelectionService` / `PreparingManager`, `ensureAutoMode`, подключение `PreparingScreen`, тела 0x50 и 0x55, отсутствие дефолта `flowRateMlPerSec`, unit-тесты, `summary.md` — см. сообщение пользователя в чате complex-оркестратора (sessionId `viwa-android-g-calibration-cooking/modules/stage-g3-cooking-flow`).
